services:
  stars_bot:
    build: .
    container_name: stars_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/data
    environment:
      - DB_PATH=/data/bot.db

volumes:
  bot_data:
